from setuptools import find_packages, setup

setup(name='drafter',
      version='1.0',
      description='layer ontop of Pango and Cairo for report generation',
      author='bibekdahal',
      packages=find_packages(),
      )
